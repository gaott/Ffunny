apply plugin: 'java'
apply plugin: 'eclipse'

repositories { 
	mavenCentral() 
 } 

dependencies {
	compile 'org.apache.httpcomponents:httpclient:4.4.1'
}

version = '1.0'
jar {
    manifest {
        attributes 'Implementation-Title': 'Just for fun',
                   'Implementation-Version': version
    }
}

